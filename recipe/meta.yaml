{% set name = "julia" %}
{% set version = "1.4.0" %}
{% set sha256 = "16d58992157600a644d17b00c55d501776681d564f910edf45a6ed27d27d3392" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: v{{ version }}.tar.gz
  url: https://github.com/JuliaLang/julia/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  skip: true  # [win]
  number: 0
  features:

requirements:
  build:
    - cmake-binary
    - perl
    - python 2.7.*
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - libblas
    - libcblas
    - liblapack
    - patchelf  # [linux]
    - zlib
    - openlibm 0.5.4
    - openspecfun 0.5.3
    - fftw
    - gmp
    - mpfr
    - curl
    - libgit2 0.27
    - libosxunwind  # [osx]
    - arpack
    - suitesparse
    - pcre2

  run:
    - zlib
    - {{ pin_compatible("libgit2", max_pin="x.x") }}
    - libosxunwind  # [osx]
    - fftw
    - gmp
    - mpfr
    - openlibm 0.5.4
    - openspecfun 0.5.3
    - arpack
    - suitesparse
    - curl
    - pcre2

test:
  commands:
    - julia -e 'Base.runtests("LibGit2/online Pkg/pkg")'
  requires:
    - perl

about:
  home: http://julialang.org/
  license: MIT
  license_file: LICENSE.md
  summary: A high-performance dynamic programming language for data science

extra:
  recipe-maintainers:
    - SylvainCorlay
    - bgruening
    - dfornika
    - acaprez
    - mariusvniekerk
    - abhi18av
