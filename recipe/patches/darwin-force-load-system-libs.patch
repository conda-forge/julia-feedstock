--- Make.inc
+++ Make.inc
@@ -1497,7 +1497,7 @@
 
 # --whole-archive
 ifeq ($(OS), Darwin)
-  whole_archive = -Xlinker -force_load $(1)
+  whole_archive = $(if $(filter -l%,$(1)),$(1),-Xlinker -force_load $(1))
   WHOLE_ARCHIVE := -Xlinker -all_load
   NO_WHOLE_ARCHIVE :=
 else ifeq ($(OS), WINNT)
